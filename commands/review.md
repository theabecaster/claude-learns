# /claude-learns:review

Read the `## Learned Preferences` section of `CLAUDE.md` in the current project directory.

List all rules with their dates in this format:
```
N. [YYYY-MM-DD] <rule text>
```

Then show the total count: "**Total: N learned preferences**"

If the section doesn't exist or is empty, say: "No learned preferences recorded yet."

Remind the user: "You can edit CLAUDE.md directly to remove any rule."
